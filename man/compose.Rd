% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{compose}
\alias{compose}
\title{Compose multiple glyph specs into a layout}
\usage{
compose(
  ...,
  type = "hstack",
  widths = NULL,
  heights = NULL,
  gap = 10,
  shared_scales = FALSE,
  linked_selections = FALSE,
  title = NULL
)
}
\arguments{
\item{...}{glyph_spec objects or layout objects (for nesting)}

\item{type}{Layout type: "hstack", "vstack", "grid", "wrap"}

\item{widths}{Relative widths for columns (e.g. c(2, 1) for 2:1 split)}

\item{heights}{Relative heights for rows}

\item{gap}{Gap between plots in px}

\item{shared_scales}{Share axis scales across plots: TRUE, FALSE,
"x", "y", or "both"}

\item{linked_selections}{Share interaction selections across plots}

\item{title}{Overall layout title}
}
\value{
A \code{glyph_layout} object
}
\description{
Compose multiple glyph specs into a layout
}
\examples{
p1 <- glyph(mtcars, x = wt, y = mpg) |> mark_point()
p2 <- glyph(mtcars, x = wt, y = hp) |> mark_point()
p3 <- glyph(mtcars, x = hp, y = mpg) |> mark_line()

# Horizontal stack
compose(p1, p2, type = "hstack")

# Grid with linked brushing
compose(p1, p2, p3, type = "wrap",
        linked_selections = TRUE, shared_scales = "x")
}
