% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_text}
\alias{mark_text}
\title{Add a text/label mark}
\usage{
mark_text(spec, ..., data = NULL, style = list(), smart_repel = TRUE)
}
\arguments{
\item{spec}{A glyph_spec}

\item{...}{Aesthetic mappings (x, y, color, size, shape, alpha, tooltip)}

\item{data}{Optional per-mark data override}

\item{style}{Named list of fixed visual properties}

\item{smart_repel}{Automatically avoid label overlaps (TRUE by default).
Inspired by \pkg{ggrepel}, but simpler: rather than a full force-directed
simulation, glyph resolves overlaps by iterative pairwise nudging —
repeatedly comparing every pair of label bounding boxes and, for any
pair that still overlaps, pushing them a small step apart vertically
(up to 50 passes, stopping as soon as none overlap). This is a
first-class feature, not an extension package.}
}
\value{
Modified \code{glyph_spec} object with the text mark added
}
\description{
Add a text/label mark
}
